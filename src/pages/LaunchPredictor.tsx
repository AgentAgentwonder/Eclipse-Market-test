import { LaunchPredictorPanel } from '../components/launchPredictor';

export default function LaunchPredictor() {
  return (
    <div className="min-h-screen bg-slate-950">
      <LaunchPredictorPanel />
    </div>
  );
}
